<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SelfCheckoutApp.Pages.ReceiptDetailsPage"
    Title="Receipt Details">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="{Binding ReceiptId, StringFormat='Receipt ID: {0}'}" FontSize="20" />
            <Label Text="{Binding PurchaseDate, StringFormat='Purchased on: {0:dd MMM yyyy HH:mm}'}" />
            <Label Text="{Binding Total, StringFormat='Total: £{0:F2}'}" />
            <CollectionView ItemsSource="{Binding ReceiptItems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout>
                            <Label Text="{Binding ProductId}" />
                            <Label Text="{Binding Quantity, StringFormat='Qty: {0}'}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
